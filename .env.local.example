# Machine-local env - copy to .env.local and customise
# This file is tracked; .env.local is not.

export GH_DEFAULT_USER=angusb93       # personal
# export GH_DEFAULT_USER=angus-msquared  # work

# export PALETTE=bathory               # override default theme

# Secret references are op:// pointers, not secrets. Override only if the key
# moves in 1Password; see "Secrets (1Password)" in README.md.
# export OPENROUTER_KEY_REF="op://Private/<item-id>/credential"
