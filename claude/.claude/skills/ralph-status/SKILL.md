---
name: ralph-status
description: Show Ralph track status
user-invocable: true
allowed-tools:
  - Bash
---

# Ralph Status

Run the `ralph-status` command and present the output to the user.

```bash
ralph-status $ARGUMENTS
```

Summarize the track status clearly. If the command fails, help the user troubleshoot (e.g. no active track, ralph-status not on PATH).
