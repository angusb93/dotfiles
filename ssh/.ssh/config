Host morty
  # Direct LAN route (home network only) - the fast local path, default.
  # NOTE: 192.168.1.96 is a DHCP lease - pin a static reservation for stability.
  HostName 192.168.1.96
  User angus

Host morty-ts
  # Tailscale route - works anywhere (NordVPN kill-switch must be off).
  HostName nas.taile1ace0.ts.net
  User angus
